{
    "name": "particle-cf-bridge",
    "version": "1.0.0",
    "description": "A cloud function that bridges pubsub to BigQuery.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@google-cloud/bigquery": "^5.9.1",
        "colors": "^1.1.2",
        "util": "^0.10.3"
    }
}
